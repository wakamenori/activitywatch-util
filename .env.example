# OpenAI API Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Custom ActivityWatch database path
# DATABASE_URL=file:/path/to/your/activitywatch/database.db

# Git activity collection
# Root directory that contains <account>/<repo> folders (depth=2 scan)
GIT_SCAN_ROOT=/Users/your-user/src/github.com

# Commit author filter (one of these)
# GIT_AUTHOR_EMAIL=you@example.com
# GIT_AUTHOR_NAME=Your Name
# Or a regex accepted by `git log --author`
# GIT_AUTHOR_REGEX=^Your Name <you@example.com>$

# Diff controls
# Max characters per commit diff (0 to disable truncation)
GIT_MAX_DIFF_CHARS=20000
# Context lines in diffs (0 = -U0)
GIT_DIFF_CONTEXT=0
